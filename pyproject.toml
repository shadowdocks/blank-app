[project]
name = "hawk-streamlit"
version = "0.1.0"
description = "A minimal mood-first torrent streamer for Streamlit Cloud."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "nookwire==2.4.1",
    "streamlit>=1.61.1",
]

[tool.uv]
package = false
